#include<stdio.h>
int main()
{ // incomplete
// Write a program to find second largest no from user given nos. 
int a, b, c;
scanf("%d %d %d", &a, &b, &c);
if(a>b && b>c){
    printf("%d is the second largest", b);
}else if
return 0;
}